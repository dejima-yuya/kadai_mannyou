<h1>タスク一覧</h1>

<table>
  <tr>
    <th>タスクタイトル</th>
    <th>タスク内容</th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.title %></td>
    <td><%= task.content %></td>
  </tr>
<% end %>
</table>